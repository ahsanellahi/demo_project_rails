<%= form_for([product, review], remote: true) do |f| %>
  <div class="field">
    <%= f.label :content %><br />
    <%= f.text_area :content, required: true %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
